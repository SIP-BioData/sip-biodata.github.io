import Breadcrumbs from '@/components/Navigation/Breadcrumbs'
import Layout from '@/components/Layout/Layout'
import LowerPageLayout from '@/components/Layout/LowerPageLayout'
import MdxLayout from '@/components/Layout/MdxLayout'
import GroupDetailList from '@/components/Elements/GroupDetailList'
import BackToDataLink from '@/components/Navigation/BackToDataLink'
import Image from 'next/image'
import { getSipDatabaseStaticProps } from '@/lib/static'

export const getStaticProps = getSipDatabaseStaticProps

export const groupId = '1C'
export const groupName = '農業環境エンジニアリング'
export const breadcrumbs = [
  {
    label: '研究グループ',
    path: '/group',
  },
  {
    label: groupName,
  },
]

# 研究グループ：農業環境エンジニアリング

環境共存型農業を実現するため、バイオデジタル化技術により新しい評価軸となる農業環境エンジニアリングシステムを構築します。

[https://www.naro.go.jp/laboratory/brain/sip/sip2/theme/theme01.html](https://www.naro.go.jp/laboratory/brain/sip/sip2/theme/theme01.html)

---

<Image
  src="/group1C_01.png"
  alt="農業環境エンジニアリングのイメージ"
  width={540}
  height={359}
  priority
/>

---

## データ一覧

export default (props) => {
    const data = props.database.filter((v) => v.sip_group_id === groupId)
  return (
    <Layout title={groupName}>
      {/* prettier-ignore */}
      <LowerPageLayout>
        <Breadcrumbs items={breadcrumbs} />
        <MdxLayout>
          {props.children}
          <GroupDetailList list={data} columns={props.columns} />
          <BackToDataLink />
        </MdxLayout>
      </LowerPageLayout>
    </Layout>
  )

}
