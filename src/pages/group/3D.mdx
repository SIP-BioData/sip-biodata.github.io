import Breadcrumbs from '@/components/Navigation/Breadcrumbs'
import Layout from '@/components/Layout/Layout'
import LowerPageLayout from '@/components/Layout/LowerPageLayout'
import MdxLayout from '@/components/Layout/MdxLayout'
import GroupDetailList from '@/components/Elements/GroupDetailList'
import BackToDataLink from '@/components/Navigation/BackToDataLink'
import Image from 'next/image'
import { getDatabaseStaticProps } from '@/lib/static'

export const getStaticProps = getDatabaseStaticProps

export const groupId = '3D'
export const groupName = 'スマートバイオプロセス'
export const breadcrumbs = [
  {
    label: '研究グループ',
  },
  {
    label: groupName,
  },
]

# 研究グループ：スマートバイオプロセス

廃水処理予測診断技術の確立により日本国内のバイオプロセス産業全体のコスト低減と国際競争力強化に貢献します。

[https://www.naro.go.jp/laboratory/brain/sip/sip2/theme/theme05.html](https://www.naro.go.jp/laboratory/brain/sip/sip2/theme/theme05.html)

---

<Image
  src="/group3D_01.png"
  alt="スマートバイオプロセスのイメージ"
  width={834}
  height={490}
  priority
/>

---

## データ一覧

export default (props) => {
  const data = props.sipDatabase.filter((v) => v.group_id === groupId)
  return (
    <Layout title={groupName}>
      {/* prettier-ignore */}
      <LowerPageLayout>
        <Breadcrumbs items={breadcrumbs} />
        <MdxLayout>
          {props.children}
          <GroupDetailList list={data} />
          <BackToDataLink />
        </MdxLayout>
      </LowerPageLayout>
    </Layout>
  )

}
