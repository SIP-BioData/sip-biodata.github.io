import Breadcrumbs from '@/components/Navigation/Breadcrumbs'
import Layout from '@/components/Layout/Layout'
import LowerPageLayout from '@/components/Layout/LowerPageLayout'
import MdxLayout from '@/components/Layout/MdxLayout'
import GroupDetailList from '@/components/Elements/GroupDetailList'
import BackToDataLink from '@/components/Navigation/BackToDataLink'
import Image from 'next/image'
import { getSipDatabaseStaticProps } from '@/lib/static'

export const getStaticProps = getSipDatabaseStaticProps

export const groupId = '1A'
export const groupName = 'データ駆動型育種'
export const breadcrumbs = [
  {
    label: '研究グループ',
    path: '/group',
  },
  {
    label: groupName,
  },
]

# 研究グループ：データ駆動型育種

大量の育種データを取得・蓄積・解析するための基盤を構築しました。現在、蓄積したゲノム情報とデジタル化した形質情報に基づき、遺伝子型と形質の関連モデルを構築しています(下図)。 本モデルを用いれば、遺伝子型から形質を高精度で予測できるため、選抜効率を大幅に向上できます。

[https://www.naro.go.jp/laboratory/brain/sip/sip2/theme/theme2102.html#t01](https://www.naro.go.jp/laboratory/brain/sip/sip2/theme/theme2102.html#t01)

---

<Image
  src="/group1A_01.png"
  alt="データ駆動型育種のイメージ"
  width={540}
  height={395}
  priority
/>

---

## データ一覧

export default (props) => {
  const data = props.database.filter((v) => v.sip_group_id === groupId)
  return (
    <Layout title={groupName}>
      {/* prettier-ignore */}
      <LowerPageLayout>
        <Breadcrumbs items={breadcrumbs} />
        <MdxLayout>
            {props.children}
            <GroupDetailList list={data} columns={props.columns} />
            <BackToDataLink />
        </MdxLayout>
      </LowerPageLayout>
    </Layout>
  )
}
