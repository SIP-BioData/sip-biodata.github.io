import Breadcrumbs from '@/components/Navigation/Breadcrumbs'
import Layout from '@/components/Layout/Layout'
import LowerPageLayout from '@/components/Layout/LowerPageLayout'
import MdxLayout from '@/components/Layout/MdxLayout'
import GroupDetailList from '@/components/Elements/GroupDetailList'
import BackToDataLink from '@/components/Navigation/BackToDataLink'
import Image from 'next/image'
import { getSipDatabaseStaticProps } from '@/lib/static'

export const getStaticProps = getSipDatabaseStaticProps

export const groupId = '4Amicrobe'
export const groupName = '微生物探索プラットフォーム'
export const breadcrumbs = [
  {
    label: '研究グループ',
  },
  {
    label: groupName,
  },
]

# 研究グループ：微生物探索プラットフォーム

我が国のスマートバイオ産業の拡大と関連するバイオテクロノジー産業の発展および国際競争力の強化を図るため、未利用微生物資源の確保と利用のための技術プラットフォームを構築することを目指します。本技術により、未知/未活用微生物へのアプローチし、百万単位の微生物を取り扱い、作業効率を数十倍効率化すると共に、コストを大幅に削減することで、高機能微生物等のハイスループットかつ広域な微生物探索を実現します。

---

<Image
  src="/group4A_01.png"
  alt="スマートバイオプロセスのイメージ"
  width={847}
  height={473}
  priority
/>

---

## データ一覧

export default (props) => {
  const data = props.database.filter((v) => v.sip_group_id === groupId)
  return (
    <Layout title={groupName}>
      {/* prettier-ignore */}
      <LowerPageLayout>
        <Breadcrumbs items={breadcrumbs} />
        <MdxLayout>
          {props.children}
          <GroupDetailList list={data} columns={props.columns} />
          <BackToDataLink />
        </MdxLayout>
      </LowerPageLayout>
    </Layout>
  )
}
